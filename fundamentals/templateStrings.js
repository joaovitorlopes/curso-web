// Template strings
const nome = 'Rebeca'
const concatenacao = `Olá ${nome}!`
console.log(concatenacao)

// expressoes...
console.log(`1 + 1 = ${1 + 1}`)

const up = texto => texto.toUpperCase() // Arrow Function para uppercase
console.log(`Ei... ${up('cuidado')}!`)